<%= form_with(model: sm) do |form| %>
  <% if sm.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sm.errors.count, "error") %> prohibited this sm from being saved:</h2>

      <ul>
        <% sm.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :title %>
    <%= form.submit "Create a comment", class: "btn btn-success mx-2 mb-1" %>
  </div>

  <div class="mb-4">  
    <%= form.text_field :msg_id, value: @msg.id, type: :hidden %>
  </div>

<% end %>
